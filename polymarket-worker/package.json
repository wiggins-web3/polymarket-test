{
  "name": "polymarket-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Polymarket onboarding",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@polymarket/clob-client": "^5.1.3",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

